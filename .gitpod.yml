tasks:
  - name: Terminal 1
    before: echo 'Initializing the project the-primer'
    init: ./gradlew build -x tests
  - name: Terminal 2
    before: echo 'Launching the Service'
    init: ./gradlew run